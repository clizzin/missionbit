// Create the global game variable here. This script must come first in your index.html (after phaser.js)
// so that all your other scripts are able to use the game variable.
var game = new Phaser.Game(800, 800, Phaser.AUTO, 'game-div');
